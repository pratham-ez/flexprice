/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 3fefe1b31f2a
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";

export type DtoCreatePriceUnitRequest = {
  baseCurrency: string;
  code: string;
  conversionRate: string;
  name: string;
  precision?: number | undefined;
  symbol: string;
};

/** @internal */
export type DtoCreatePriceUnitRequest$Outbound = {
  base_currency: string;
  code: string;
  conversion_rate: string;
  name: string;
  precision?: number | undefined;
  symbol: string;
};

/** @internal */
export const DtoCreatePriceUnitRequest$outboundSchema: z.ZodMiniType<
  DtoCreatePriceUnitRequest$Outbound,
  DtoCreatePriceUnitRequest
> = z.pipe(
  z.object({
    baseCurrency: z.string(),
    code: z.string(),
    conversionRate: z.string(),
    name: z.string(),
    precision: z.optional(z.int()),
    symbol: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      baseCurrency: "base_currency",
      conversionRate: "conversion_rate",
    });
  }),
);

export function dtoCreatePriceUnitRequestToJSON(
  dtoCreatePriceUnitRequest: DtoCreatePriceUnitRequest,
): string {
  return JSON.stringify(
    DtoCreatePriceUnitRequest$outboundSchema.parse(dtoCreatePriceUnitRequest),
  );
}

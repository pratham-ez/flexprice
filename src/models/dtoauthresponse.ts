/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 305a979fada8
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type DtoAuthResponse = {
  tenantId?: string | undefined;
  token?: string | undefined;
  userId?: string | undefined;
};

/** @internal */
export const DtoAuthResponse$inboundSchema: z.ZodMiniType<
  DtoAuthResponse,
  unknown
> = z.pipe(
  z.object({
    tenant_id: types.optional(types.string()),
    token: types.optional(types.string()),
    user_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "tenant_id": "tenantId",
      "user_id": "userId",
    });
  }),
);

export function dtoAuthResponseFromJSON(
  jsonString: string,
): SafeParseResult<DtoAuthResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoAuthResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoAuthResponse' from JSON`,
  );
}

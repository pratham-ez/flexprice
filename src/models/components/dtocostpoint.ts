/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoCostPoint = {
  cost?: string | undefined;
  eventCount?: number | undefined;
  quantity?: string | undefined;
  timestamp?: string | undefined;
};

/** @internal */
export const DtoCostPoint$inboundSchema: z.ZodMiniType<DtoCostPoint, unknown> =
  z.pipe(
    z.object({
      cost: types.optional(types.string()),
      event_count: types.optional(types.number()),
      quantity: types.optional(types.string()),
      timestamp: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "event_count": "eventCount",
      });
    }),
  );

export function dtoCostPointFromJSON(
  jsonString: string,
): SafeParseResult<DtoCostPoint, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoCostPoint$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoCostPoint' from JSON`,
  );
}

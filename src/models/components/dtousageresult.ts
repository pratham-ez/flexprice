/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoUsageResult = {
  value?: number | undefined;
  windowSize?: string | undefined;
};

/** @internal */
export const DtoUsageResult$inboundSchema: z.ZodMiniType<
  DtoUsageResult,
  unknown
> = z.pipe(
  z.object({
    value: types.optional(types.number()),
    window_size: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "window_size": "windowSize",
    });
  }),
);

export function dtoUsageResultFromJSON(
  jsonString: string,
): SafeParseResult<DtoUsageResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoUsageResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoUsageResult' from JSON`,
  );
}

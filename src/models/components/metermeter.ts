/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MeterAggregation,
  MeterAggregation$inboundSchema,
} from "./meteraggregation.js";
import { MeterFilter, MeterFilter$inboundSchema } from "./meterfilter.js";
import {
  TypesResetUsage,
  TypesResetUsage$inboundSchema,
} from "./typesresetusage.js";
import { TypesStatus, TypesStatus$inboundSchema } from "./typesstatus.js";

export type MeterMeter = {
  aggregation?: MeterAggregation | undefined;
  createdAt?: string | undefined;
  createdBy?: string | undefined;
  /**
   * EnvironmentID is the environment identifier for the meter
   */
  environmentId?: string | undefined;
  /**
   * EventName is the unique identifier for the event that this meter is tracking
   *
   * @remarks
   * It is a mandatory field in the events table and hence being used as the primary matching field
   * We can have multiple meters tracking the same event but with different filters and aggregation
   */
  eventName?: string | undefined;
  /**
   * Filters define the criteria for the meter to be applied on the events before aggregation
   *
   * @remarks
   * It also defines the possible values on which later the charges will be applied
   */
  filters?: Array<MeterFilter> | undefined;
  /**
   * ID is the unique identifier for the meter
   */
  id?: string | undefined;
  /**
   * Name is the display name of the meter
   */
  name?: string | undefined;
  resetUsage?: TypesResetUsage | undefined;
  status?: TypesStatus | undefined;
  tenantId?: string | undefined;
  updatedAt?: string | undefined;
  updatedBy?: string | undefined;
};

/** @internal */
export const MeterMeter$inboundSchema: z.ZodMiniType<MeterMeter, unknown> = z
  .pipe(
    z.object({
      aggregation: types.optional(MeterAggregation$inboundSchema),
      created_at: types.optional(types.string()),
      created_by: types.optional(types.string()),
      environment_id: types.optional(types.string()),
      event_name: types.optional(types.string()),
      filters: types.optional(z.array(MeterFilter$inboundSchema)),
      id: types.optional(types.string()),
      name: types.optional(types.string()),
      reset_usage: types.optional(TypesResetUsage$inboundSchema),
      status: types.optional(TypesStatus$inboundSchema),
      tenant_id: types.optional(types.string()),
      updated_at: types.optional(types.string()),
      updated_by: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "created_at": "createdAt",
        "created_by": "createdBy",
        "environment_id": "environmentId",
        "event_name": "eventName",
        "reset_usage": "resetUsage",
        "tenant_id": "tenantId",
        "updated_at": "updatedAt",
        "updated_by": "updatedBy",
      });
    }),
  );

export function meterMeterFromJSON(
  jsonString: string,
): SafeParseResult<MeterMeter, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MeterMeter$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MeterMeter' from JSON`,
  );
}

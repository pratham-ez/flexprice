/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type DtoTaxRateOverride = {
  autoApply?: boolean | undefined;
  currency: string;
  metadata?: { [k: string]: string } | undefined;
  priority?: number | undefined;
  taxRateCode: string;
};

/** @internal */
export type DtoTaxRateOverride$Outbound = {
  auto_apply: boolean;
  currency: string;
  metadata?: { [k: string]: string } | undefined;
  priority?: number | undefined;
  tax_rate_code: string;
};

/** @internal */
export const DtoTaxRateOverride$outboundSchema: z.ZodMiniType<
  DtoTaxRateOverride$Outbound,
  DtoTaxRateOverride
> = z.pipe(
  z.object({
    autoApply: z._default(z.boolean(), true),
    currency: z.string(),
    metadata: z.optional(z.record(z.string(), z.string())),
    priority: z.optional(z.int()),
    taxRateCode: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      autoApply: "auto_apply",
      taxRateCode: "tax_rate_code",
    });
  }),
);

export function dtoTaxRateOverrideToJSON(
  dtoTaxRateOverride: DtoTaxRateOverride,
): string {
  return JSON.stringify(
    DtoTaxRateOverride$outboundSchema.parse(dtoTaxRateOverride),
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DtoInvoiceLineItemPreview,
  DtoInvoiceLineItemPreview$inboundSchema,
} from "./dtoinvoicelineitempreview.js";

export type DtoInvoicePreview = {
  /**
   * currency is the currency for all amounts
   */
  currency?: string | undefined;
  /**
   * due_date is when the invoice would be due
   */
  dueDate?: string | undefined;
  /**
   * line_items contains preview of line items
   */
  lineItems?: Array<DtoInvoiceLineItemPreview> | undefined;
  /**
   * subtotal is the subtotal amount before taxes
   */
  subtotal?: string | undefined;
  /**
   * tax_amount is the total tax amount
   */
  taxAmount?: string | undefined;
  /**
   * total is the total amount including taxes
   */
  total?: string | undefined;
};

/** @internal */
export const DtoInvoicePreview$inboundSchema: z.ZodMiniType<
  DtoInvoicePreview,
  unknown
> = z.pipe(
  z.object({
    currency: types.optional(types.string()),
    due_date: types.optional(types.string()),
    line_items: types.optional(
      z.array(DtoInvoiceLineItemPreview$inboundSchema),
    ),
    subtotal: types.optional(types.string()),
    tax_amount: types.optional(types.string()),
    total: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "due_date": "dueDate",
      "line_items": "lineItems",
      "tax_amount": "taxAmount",
    });
  }),
);

export function dtoInvoicePreviewFromJSON(
  jsonString: string,
): SafeParseResult<DtoInvoicePreview, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoInvoicePreview$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoInvoicePreview' from JSON`,
  );
}

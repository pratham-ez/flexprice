/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoEnvironmentResponse = {
  createdAt?: string | undefined;
  id?: string | undefined;
  name?: string | undefined;
  type?: string | undefined;
  updatedAt?: string | undefined;
};

/** @internal */
export const DtoEnvironmentResponse$inboundSchema: z.ZodMiniType<
  DtoEnvironmentResponse,
  unknown
> = z.pipe(
  z.object({
    created_at: types.optional(types.string()),
    id: types.optional(types.string()),
    name: types.optional(types.string()),
    type: types.optional(types.string()),
    updated_at: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "updated_at": "updatedAt",
    });
  }),
);

export function dtoEnvironmentResponseFromJSON(
  jsonString: string,
): SafeParseResult<DtoEnvironmentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoEnvironmentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoEnvironmentResponse' from JSON`,
  );
}

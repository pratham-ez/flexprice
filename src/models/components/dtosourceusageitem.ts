/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b32808e55372
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoSourceUsageItem = {
  /**
   * cost is the cost attributed to this source for the line item
   */
  cost?: string | undefined;
  /**
   * event_count is the number of events from this source (optional)
   */
  eventCount?: number | undefined;
  /**
   * percentage is the percentage of total line item cost from this source (optional)
   */
  percentage?: string | undefined;
  /**
   * source is the name of the event source
   */
  source?: string | undefined;
  /**
   * usage is the total usage amount from this source (optional, for additional context)
   */
  usage?: string | undefined;
};

/** @internal */
export const DtoSourceUsageItem$inboundSchema: z.ZodMiniType<
  DtoSourceUsageItem,
  unknown
> = z.pipe(
  z.object({
    cost: types.optional(types.string()),
    event_count: types.optional(types.number()),
    percentage: types.optional(types.string()),
    source: types.optional(types.string()),
    usage: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "event_count": "eventCount",
    });
  }),
);

export function dtoSourceUsageItemFromJSON(
  jsonString: string,
): SafeParseResult<DtoSourceUsageItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoSourceUsageItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoSourceUsageItem' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DtoProrationDetail = {
  chargeAmount?: string | undefined;
  creditAmount?: string | undefined;
  description?: string | undefined;
  lineItemId?: string | undefined;
  originalAmount?: string | undefined;
  planName?: string | undefined;
  priceId?: string | undefined;
  prorationDays?: number | undefined;
};

/** @internal */
export const DtoProrationDetail$inboundSchema: z.ZodMiniType<
  DtoProrationDetail,
  unknown
> = z.pipe(
  z.object({
    charge_amount: types.optional(types.string()),
    credit_amount: types.optional(types.string()),
    description: types.optional(types.string()),
    line_item_id: types.optional(types.string()),
    original_amount: types.optional(types.string()),
    plan_name: types.optional(types.string()),
    price_id: types.optional(types.string()),
    proration_days: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "charge_amount": "chargeAmount",
      "credit_amount": "creditAmount",
      "line_item_id": "lineItemId",
      "original_amount": "originalAmount",
      "plan_name": "planName",
      "price_id": "priceId",
      "proration_days": "prorationDays",
    });
  }),
);

export function dtoProrationDetailFromJSON(
  jsonString: string,
): SafeParseResult<DtoProrationDetail, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DtoProrationDetail$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DtoProrationDetail' from JSON`,
  );
}

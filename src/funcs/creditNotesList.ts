/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 071f71f94775
 */

import * as z from "zod/v4-mini";
import { FlexpriceCore } from "../core.js";
import { encodeFormQuery, queryJoin } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { FlexpriceError } from "../models/errors/flexpriceerror.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/httpclienterrors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/responsevalidationerror.js";
import { SDKValidationError } from "../models/errors/sdkvalidationerror.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * List credit notes with filtering
 *
 * @remarks
 * Lists credit notes with filtering
 */
export function creditNotesList(
  client: FlexpriceCore,
  creditNoteIds?: Array<string> | undefined,
  creditNoteStatus?: Array<operations.CreditNoteStatus> | undefined,
  creditNoteType?: operations.CreditNoteType | undefined,
  endTime?: string | undefined,
  expand?: string | undefined,
  invoiceId?: string | undefined,
  limit?: number | undefined,
  offset?: number | undefined,
  order?: operations.GetCreditnotesQueryParamOrder | undefined,
  sort?: string | undefined,
  startTime?: string | undefined,
  status?: operations.GetCreditnotesQueryParamStatus | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    models.DtoListCreditNotesResponse,
    | errors.ErrorsErrorResponse
    | FlexpriceError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    creditNoteIds,
    creditNoteStatus,
    creditNoteType,
    endTime,
    expand,
    invoiceId,
    limit,
    offset,
    order,
    sort,
    startTime,
    status,
    options,
  ));
}

async function $do(
  client: FlexpriceCore,
  creditNoteIds?: Array<string> | undefined,
  creditNoteStatus?: Array<operations.CreditNoteStatus> | undefined,
  creditNoteType?: operations.CreditNoteType | undefined,
  endTime?: string | undefined,
  expand?: string | undefined,
  invoiceId?: string | undefined,
  limit?: number | undefined,
  offset?: number | undefined,
  order?: operations.GetCreditnotesQueryParamOrder | undefined,
  sort?: string | undefined,
  startTime?: string | undefined,
  status?: operations.GetCreditnotesQueryParamStatus | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      models.DtoListCreditNotesResponse,
      | errors.ErrorsErrorResponse
      | FlexpriceError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.GetCreditnotesRequest | undefined = {
    creditNoteIds: creditNoteIds,
    creditNoteStatus: creditNoteStatus,
    creditNoteType: creditNoteType,
    endTime: endTime,
    expand: expand,
    invoiceId: invoiceId,
    limit: limit,
    offset: offset,
    order: order,
    sort: sort,
    startTime: startTime,
    status: status,
  };

  const parsed = safeParse(
    input,
    (value) =>
      z.parse(
        z.optional(operations.GetCreditnotesRequest$outboundSchema),
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/creditnotes")();

  const query = queryJoin(
    encodeFormQuery({
      "credit_note_ids": payload?.credit_note_ids,
      "credit_note_status": payload?.credit_note_status,
    }, { explode: false }),
    encodeFormQuery({
      "credit_note_type": payload?.credit_note_type,
      "end_time": payload?.end_time,
      "expand": payload?.expand,
      "invoice_id": payload?.invoice_id,
      "limit": payload?.limit,
      "offset": payload?.offset,
      "order": payload?.order,
      "sort": payload?.sort,
      "start_time": payload?.start_time,
      "status": payload?.status,
    }),
  );

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.apiKeyAuth);
  const securityInput = secConfig == null ? {} : { apiKeyAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "get_/creditnotes",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.apiKeyAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["400", "401", "403", "404", "4XX", "500", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    models.DtoListCreditNotesResponse,
    | errors.ErrorsErrorResponse
    | FlexpriceError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, models.DtoListCreditNotesResponse$inboundSchema),
    M.jsonErr([400, 401, 403, 404], errors.ErrorsErrorResponse$inboundSchema),
    M.jsonErr(500, errors.ErrorsErrorResponse$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}

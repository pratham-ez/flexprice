# TypesResetUsage

## Example Usage

```typescript
import { TypesResetUsage } from "@flexprice/sdk/models/components";

let value: TypesResetUsage = "NEVER";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"BILLING_PERIOD" | "NEVER" | Unrecognized<string>
```
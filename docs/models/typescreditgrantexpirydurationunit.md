# TypesCreditGrantExpiryDurationUnit

## Example Usage

```typescript
import { TypesCreditGrantExpiryDurationUnit } from "@flexprice/sdk/models";

let value: TypesCreditGrantExpiryDurationUnit = "WEEK";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"DAY" | "WEEK" | "MONTH" | "YEAR" | Unrecognized<string>
```
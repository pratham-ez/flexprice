# TypesCreditNoteType

## Example Usage

```typescript
import { TypesCreditNoteType } from "@flexprice/sdk/models";

let value: TypesCreditNoteType = "ADJUSTMENT";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"ADJUSTMENT" | "REFUND" | Unrecognized<string>
```
# Provider

provider is the integration provider name (e.g., "stripe", "razorpay")

## Example Usage

```typescript
import { Provider } from "@flexprice/sdk/models";

let value: Provider = "stripe";
```

## Values

```typescript
"stripe" | "razorpay" | "paypal"
```